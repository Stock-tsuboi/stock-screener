name: Test Workflow

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "OK"


        run: pip install -r requirements.txt

      # スクリーナー実行
      - name: Run screener
        run: python screening_ai.py

